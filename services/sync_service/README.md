# Sync service

Future service for multi-device sessions, clock alignment, websocket ingest, and cross-stream event synchronization.

It should operate on immutable streams and create derived sync annotations rather than mutating raw timestamps.
