# UI components

Future package for custom high-performance React components once Label Studio is no longer enough for a specific workflow.

The first custom candidates are dense time-series viewers, synchronized multimodal timelines, and waveform-style interval editors.
