# Android recorder

Future native recorder for higher-precision accelerometer, touch, audio, and camera-aligned capture.

The Android path should use monotonic sensor timestamps and explicit session metadata so recordings can be synchronized with other streams.
