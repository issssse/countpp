# Derived data

Local generated artifacts belong here: canonical Parquet streams, Label Studio task JSON, extractor annotations, diagnostics, and final event exports.

Raw source recordings should stay in `data/examples` for fixtures or in an immutable upload store later.
